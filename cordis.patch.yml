# dsh-plugin-gentleai-engram 的 bundle 补丁层：安装时自动把 engram 桥插入 profile 组合树。
#
# 注意：profile 侧（~/.dsh/cordis.patch.yml）不要再手写同名 insert，否则重复。
# 需要按机器覆盖配置时，在 profile patch 里对同一个 id 写 config 覆盖即可。
- insert:
    - id: engram-bridge
      name: dsh-plugin-gentleai-engram
